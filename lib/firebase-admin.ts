// Firebase Admin is temporarily disabled - using in-memory storage instead
// To re-enable, set up Firebase credentials properly in Railway

export function getAdminApp() {
    // Disabled - return null to use in-memory storage
    return null;
}

export function getAdminDb() {
    // Disabled - return null to use in-memory storage
    return null;
}
